(ns ast-editor.namespacing_test
  (:require [clojure.test :refer :all]
            [ast-editor.struct :refer :all]
            [ast-editor.core :refer :all]))

(deftest test-add-node
  (testing "should be able to add node"
    (is (= 1 1))
     ))
(run-all-tests)